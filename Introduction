Set
join連接 將兩個集合合併為一個
union並集 傳回一個包含2個集合中所有不同元素的集合
intersect交集 傳回一個僅包含2個集合共有元素的集合
difference差集 保留一個集合中不屬於另一個集呵的元素
例如:A={1,2,3,4} B={1,2} A-B={1,2} B-A={5}
symmetric difference對稱交集 保留存在於其中1個集合但不存在於2個集合中的元素
[直覺] 只在A出現和只在B出現的元素加在一起，交集的部分不要
-------------------------------------------------------------------
peek(=top):查看最上面元素，但不拿出來(通常用在stack)
front/rear:查看最前面元素/最後面元素(用在佇列)
traverse:依照特定順序拜訪每個元素(用於tree、graph、linked list)
reverse:把元素的順序完全顛倒
rotate:把元素循環移動 例如:[1,2,3,4] -左旋2次-> [3,4,1,2]
-------------------------------------------------------------------
non-primitve data structure分為
(1)Linear:Array、linked list、stacks、queues
(2)non-linear:Trees、graphs
-------------------------------------------------------------------
漸近符號（Asymptotic Notation）
🔹Big-O（O）：上界 — 最壞情況
- 表示：f(n) = O(g(n))
- 意義：存在常數 C 和 n₀，使得：
0 ≤ f(n) ≤ C·g(n)，對所有 n ≥ n₀


🔹 Omega（Ω）：下界 — 最佳情況
- 表示：f(n) = Ω(g(n))
- 意義：存在常數 C 和 n₀，使得：
0 ≤ C·g(n) ≤ f(n)，對所有 n ≥ n₀


🔹 Theta（Θ）：緊界 — 平均或精確界
- 表示：f(n) = Θ(g(n))
- 意義：當 f(n) 同時是 O(g(n)) 和 Ω(g(n)) 時成立：
Θ(g(n)) ⇔ O(g(n)) 且 Ω(g(n))
-------------------------------------------------------------------
O(1): Constant time                             🥇 最佳
O(log n): Logarithmic time                      🥈 很好
O(n): Linear time                               🥉 可接受
O(n log n): Linearithmic time                    ⚠ 中等
O(n的2次方): Quadratic time     O(n的3次方): Cubic time      ❌ 慢
O(2的n次方): Exponential time   O(n!): Factorial time  🚫 非常慢
-------------------------------------------------------------------
📦 抽象資料型別（Abstract Data Type, ADT）
✅ 定義
抽象資料型別是一種資料型別，其設計方式是將：
- 資料的表示方式
- 操作的實作方式
分離開來，讓使用者只需關注「能做什麼」，而不必知道「怎麼做」。
✅ 舉例說明
以 Stack 為例：
- 使用者只需知道 Push()、Pop()、Peek() 等操作。
- 不需知道底層是用陣列還是鏈結串
✅ 特性
- 語言獨立性：ADT 的設計不依賴特定程式語言，可在不同語言中實作。
- 封裝性：隱藏內部資料結構與實作細節，只暴露操作介面。
- 模組化：便於重複使用與維護，提升程式的可讀性與可測試性。
-------------------------------------------------------------------
演算法的五大基本特性（Properties of an Algorithm）
1.輸入
- 演算法可以有零個或多個外部提供的輸入。
- 這些輸入是演算法執行所需的初始資料。
2.輸出
- 演算法至少會產生一個輸出結果。
- 輸出是演算法對輸入資料處理後的回應。
3.明確性
- 每一個步驟都必須清楚且無歧義。
- 指令必須具備明確的語意，不能模糊或含糊。
4.有限性
- 演算法在所有情況下都必須在有限步驟內結束。
- 不允許無限迴圈或無窮遞迴。
5.有效性
- 每個指令都必須足夠基本，理論上可以用紙筆手動執行。
- 不只是定義清楚，還要「可行」與「可操作」。
