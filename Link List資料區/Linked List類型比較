鏈結串列 (Linked List) 筆記

1.Singly Linked List (單向鏈結串列)
-每個節點包含：
data: 資料欄位
next: 指向下一個節點的指標
-最後一個節點的 next 為 nullptr
-只能單向遍歷
head → [10  *] → [20  *] → [30 | nullptr]
優點：結構簡單，記憶體使用效率高；適合插入、刪除操作 (比陣列更快)
限制：無法反向遍歷；插入/刪除需追蹤前一節點

---

2.Doubly Linked List (雙向鏈結串列)
-每個節點包含：
data: 資料欄位
prev: 指向前一個節點的指標
next: 指向下一個節點的指標
- 可雙向遍歷
nullptr <- [10] ⇄ [20] ⇄ [30] -> nullptr
優點：可從任一方遍歷；插入與刪除更靈活 (不須追蹤前一節點)
限制：每個節點需額外儲存 prev 指標 (記憶體使用較高)；操作邏輯較複雜


---
3.Circular Linked List (環狀鏈結串列)
單向環狀
[10] → [20] → [30]
↑             ↓
←←←←←←←←←←←←←←←
雙向環狀
[10] ⇄ [20] ⇄ [30]
↑                 ↓
←←←←←←←←←←←←←←←←←←
優點：適合環狀排程 (如 CPU 時間片輪轉)；不須判斷結尾，遍歷可無限循環
限制：須小心避免無限循環；操作邏輯更複雜

