                  陣列   vs.  鏈結串列
記憶體配置 連續的記憶體區塊 / 非連續，每個節點動態配置
大小彈性   固定大小 / 動態大小，可在執行時增減
存取方式   直接透過索引存取O(1) / 只能從頭開始逐一存取O(n)
插入／刪除效率 成本高 → 需搬移元素，時間複雜度 O(n) / 若已知節點指標，插入／刪除效率高 → O(1)
記憶體使用 無額外負擔（只儲存資料）/ 每個節點需額外儲存指標（如 next，有時還有 prev）
快取效能 較佳 → 記憶體連續，快取命中率高 / 較差 → 節點分散，快取命中率低
實作難易度 較簡單，容易撰寫與使用 / 較複雜，需處理指標與記憶體管理
適用情境 適合資料大小已知，且需頻繁隨機存取的情況 / 適合資料大小不固定，且需頻繁插入／刪除的情況
索引存取 O(1)/O(n)
搜尋元素 O(n)/O(n)
插入至開頭 O(n)/O(1)
插入至中間 O(n)/O(n)
插入至尾端 O(1)/O(n)unless tail pointer is used .O(1) With tail pointer and singly linked list
刪除 O(n)/O(n). O(1)If node pointer is known
