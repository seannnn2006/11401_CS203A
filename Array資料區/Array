# 📦 資料結構筆記：陣列 (Array)

### ✅ 定義與特性
* **定義**：陣列是一種資料集合，元素儲存在**連續的記憶體位置**中。
* **存取機制**：每個元素都可以透過 **索引 (Index)** 直接存取。

#### 核心特性
1. **固定大小**：陣列在建立時就決定大小，無法動態擴充。
2. **常數時間存取**：存取任意位置元素的時間複雜度為 $O(1)$。
3. **同質性**：所有元素通常為相同資料型別，提升效率。
4. **位置導向**：適合根據位置快速讀取或更新資料。

---

### 📐 多維陣列宣告 (Multi-dimensional Array)
```cpp
// 2D Array: 3 rows (直) x 4 columns (橫)
int array2d[3][4]; 

// 3D Array: 2 planes (高/blocks), 3 rows, 4 cols
int array3d[2][3][4];
--------------------------------------------------------------
## 🧩 ADT：Array（陣列）

### ✅ 物件（Objects）
* 陣列是一組 `<index, value>` 的配對集合。  
* 每個 index 都對應一個 value。  
* index 是有限且有序的集合，可以是一維、二維或多維。  

---

### ✅ 函式（Functions）
- **Array Create(j, list)**  
  * 建立一個 j 維陣列  
  * `list` 是一個 j-tuple，表示每個維度的大小  
  * 陣列中的項目初始為未定義  

- **Item Retrieve(A, i)**  
  * 從陣列 A 中取出索引 i 對應的項目  
  * i 必須是合法的 index  

- **Array Store(A, i, x)**  
  * 將項目 x 儲存到陣列 A 的索引 i 位置  
  * 更新該位置的值為 x  

---

## 🆚 靜態陣列 vs 動態陣列

| 特性             | 靜態陣列 (Static Array) | 動態陣列 (Dynamic Array) |
|------------------|-------------------------|--------------------------|
| **大小決定時機** | 編譯時期 (compile-time) | 執行時期 (runtime)       |
| **儲存位置**     | Stack                   | Heap                     |
| **記憶體釋放**   | ❌ 不可釋放              | ✅ 可釋放                 |

---
## 📌 動態陣列（Dynamic Array）筆記

### 🔹 宣告與初始化
```cpp
int *array; // 宣告一個整數指標 array (指向一塊記憶體區域，用來儲存整數陣列)
int n = 10;
array = (int *) malloc(n * sizeof(int)); 

## 📌 動態陣列擴充 (realloc)

### 🔹 擴充容量
```cpp
n = n * 2; // 將原本的陣列大小 n 乘以 2，準備擴充為 2 倍容量
array = (int *) realloc(array, n * sizeof(int)); // 使用 realloc 擴充原本的記憶體空間

## 📌 realloc 是否改變起始位址？

### ✅ 可能保持不變
- 若原記憶體區塊後方有足夠空間可擴充，`realloc` 會直接延伸原區塊。  
- 此時，**起始位址不變**，資料也不需搬移。  

### ❌ 可能改變
- 若原區塊後方空間不足，`realloc` 會：  
  - 在其他位置分配一塊更大的記憶體。  
  - 將原資料複製到新區塊。  
  - ⚠ 釋放原記憶體。  
- 此時，**起始位址會改變**，必須使用 `realloc` 的回傳值更新指標。  


# 📌 C++ STL Vector 常用操作筆記

## 🔹 迭代器 (Iterators)
- `begin()`：指向第一個元素  
- `rbegin()`：指向最後一個元素  
- `end()`：指向最後一個元素的下一個位置  
- `rend()`：指向第一個元素的前一個位置  

---

## 🔹 記憶體管理
- `shrink_to_fit()`：釋放未使用的記憶體，減少空間浪費  

---

## 🔹 元素操作

### push_back()
加入一個已建構好的物件  
```cpp
Person p1("Alice", 30); 
people.push_back(p1);  // 複製 p1 進 vector

--------------------------------------------------------------
🔹 Insertion Sort 作法
🔧 步驟：
- 從第 1 個元素開始。
- 與前面元素比較，向左移動直到找到位置。
- 插入該元素。
- 重複直到結束。
🔹 Selection Sort 作法
🔧 步驟：
- 從第 0 個元素開始，找出剩下元素中的最小值。
- 與目前位置交換。
- 重複直到最後一個元素。
--------------------------------------------------------------
char string[6] = "Hello";     // string (with '\0' at the end)
char *words[3] = {"cat", "dog", "fish"};   // array of strings
--------------------------------------------------------------
 Unsorted array
 ◦ Linear search O(n)
 Sorted array
 ◦ Linear search O(n)
 ◦ Binary search O(logn)
--------------------------------------------------------------
Access、Update O(1)
Insert、Delete、Traverse O(n)
