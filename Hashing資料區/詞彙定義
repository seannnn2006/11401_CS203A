1. Key:用來辨識資料的唯一標籤，通常由Value₁經由hash function  f(Value₁)推導而來
2. Value₂:實際要儲存或查詢資料的內容，是我們關心的資訊本體
3. Computed Index:由f(Value₁) 計算出的整數索引，用來決定資料在Hash Table中的位置
4. Collision:多個不同的(Value₁)經由f()映射到相同的Index，導致資料重疊
5. Goal:確保key的唯一性，並能高效的查詢、插入、刪除對應的 Value₂

🔄 運作流程：Key → Hash Function → Index → Value₂
Key又是由f(Value₁)導出

Key Concept
1. Hash Table: 一種陣列結構，用來儲存Key-value pair，支援快速查詢、插入、刪除操作
2. Key:使用者提供的識別值(EX:學號、帳號)，用來定位資料
3. Hash Function:將key轉換成整數index，決定資料在Hash Table中的儲存位置
4. Collision:當兩個不同的key經由hash function映射到相同index時發生碰撞
5. Load Factor(α):表示表格的使用率:α=元素數量/表格大小，衡量Hash Table的飽和程度
