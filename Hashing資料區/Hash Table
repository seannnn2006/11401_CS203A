🛠️定義:Hash Table(雜湊表) 是一種"鍵值對"儲存資料的資料結構，能夠提供快速插入、刪除、查詢操作
🛠️底層結構:
1.Array:用於儲存bucket，及雜湊後的索引位置
2.Linked List:用於處理碰撞(collision)，每個bucket儲存一個鏈結串列
🛠️Hybrid結構:Hash Table = Array + Linked List
🛠️雜湊與碰撞處理:
Hash Function:將key(鍵)轉換為索引陣列(bucket index)
碰撞處理方式:
1.使用Linked List儲存具有相同雜湊值的多筆資料
2.每個bucket對應一個鏈結串列，儲存所有雜湊到該位置的項目
🛠️特性:
1.Hash Table結合陣列的快速存取與鏈結串列的彈性儲存
2.雜湊函數的設計與碰撞處理策略是影響效能的關聯
3.常見應用:字典、快取系統、資料庫索引等等......
🛠️名詞定義:
1.key:唯一識別資料的標籤，例如名字、ID、編號
2.value:與key對應的資料內容，例如:分數、設定值
3.hash:將key轉換為整數索引的函數運算結果
4.bucket:儲存資料的陣列位置，由hash決定
