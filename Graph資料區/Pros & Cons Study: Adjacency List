Adjacency List定義:鄰接串列是一種圖的表示方式，每個頂點只存放他的鄰居(相鄰頂點)
優點:
1. O(V+E)空間複雜度:對稀疏圖非常高效
2. 快速遍歷:存取某頂點的鄰居只需O(deg(v))
3. 適合BFS/DFS，整體時間複雜度O(V+E)
4. 容易擴展到大型圖(可處理數百萬節點)
5. 插入/刪除邊操作只需O(1)
缺點:
1. 檢查某條邊(u,v)是否存在需O(deg(u))
2. 實作稍複雜(需要節點與指標結構)
3. 若使用大量小型鏈節節點，可能有額外記憶體開銷
適用場景:
1. 稀疏圖(大多現實世界的圖:社交網路、地圖)
2. 適合BFS/DFS、Dijkstra、Prim、Kruskal等演算法
3. 大型、動態圖
