1.定義:堆疊樹是一種完全二元樹(Complete Binary Tree)，並且滿足堆疊順序性(Heap-order property)
常見的兩種堆:
(1)最大堆(Max-Heap):每個父節點的值≥子節點的值，根節點是全域最大值 (parent ≥ children)
(2)最小堆(Min-Heap):每個父節點的值≤子節點的值，根節點是全域最小值 (parent ≤ children)

2.形狀特性:
Heap必須是完全二元樹:
(1)除了最後一層外，每一層都必須填滿
(2)最後一層的節點必須由左至右依序填入
此特性可保證Heap高效儲存在陣列中，不會有空洞

3.用途:常用於
(1)優先佇列(priority queues)
(2)堆疊序(heap sort)
