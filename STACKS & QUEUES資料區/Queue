# Queue (佇列)

##  基本概念
- 一種 **線性資料結構**，遵循 **先進先出 (FIFO)** 的原則。  
- 特性：
  1. 元素從一端進入 (**rear**)。  
  2. 元素從另一端移除 (**front**)。  
- 可以想像成排隊的人潮：最先排隊的人最先被服務。  

---

## 常見操作
- **enqueue(x)**：將元素 `x` 加入佇列尾端 (rear)。  
- **dequeue()**：移除佇列前端 (front) 的元素。  
- **peek()**：查看佇列前端元素但不移除。  
- **isEmpty()**：判斷佇列是否為空。  
- **isFull()**：判斷佇列是否已滿。  

---

## 時間複雜度
| 操作       | 時間複雜度 |
|------------|------------|
| enqueue    | O(1)       |
| dequeue    | O(1)       |
| peek       | O(1)       |
| isEmpty    | O(1)       |
| isFull     | O(1)       |

---

##  應用範例
- **排隊系統**：模擬人潮或資源分配。  
- **作業系統**：CPU 排程。  
- **演算法**：BFS (Breadth-First Search) 使用 Queue 來管理。  
