Scenario 1: Only Entrance for Input and Output 

(若由index=0作為bottom的情況)
top==0:stack is empty(一開始)
top=max_size-1:stack is full(填滿)
Array storage expansion by realloc():only max_size should be modified
----------------------------------------------------------------------
Scenario 2: One Entrance, One Exit

If we choose “a” as front(左), “b” will be the rear(右).
一開始front=-1、rear=-1
