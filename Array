✅ 定義
- 陣列是一種資料集合，元素儲存在連續的記憶體位置中。
- 每個元素都可以透過**索引（index）**直接存取。
✅ 特性
1.固定大小:陣列在建立時就決定大小，無法動態擴充
2.常數時間存取:存取任意位置元素的時間複雜度完O(1)
3.同質性:所有元素通常為相同資料型別，提升效率
4.位置導向:適合根據位置快速讀取或更新資料
--------------------------------------------------------------
int array2d[3][4]; //2D array with 3 rows(直) and 4 columns(橫)
int array3d[2][3][4]; //3D array:2是高(planes，blocks)，3是rows，4是cols
--------------------------------------------------------------
ADT：Array（陣列）
✅ 物件（Objects）
陣列是一組<index,value>的配對集合
每個index都對應一個value，其中:index是有限且有序的集合，可以是一維、二維或多維
✅ 函式（Functions）
🔹Array creat(j,list)
- 建立一個j維陣列
- list是一個j-tuple，表示每個維度的大小
- 陣列中的項目初始為未定義
🔹Item Retrieve(A,i)
- 從陣列A中取出索引i對應的項目
- i必須是合法的index
🔹Array Store(A,i,x)
- 將項目x儲存到陣列A的索引i位置
- 更新該位置的值是x
--------------------------------------------------------------
特性                靜態陣列                 動態陣列
大小決定時機     編譯時期(compile)       執行時期(runtime)
儲存位置             Stack                     Heap
記憶體釋放              ❌                      ✅
--------------------------------------------------------------
動態陣列（Dynamic Array）筆記
int *array; //宣告一個整數指標array(它將指向一塊記憶體區域，用來儲存整數陣列)
int n = 10;
array = (int *) malloc(n * sizeof(int)); //使用malloc在heap中分配一塊記憶體，大小為n的整數
// sizeof(int) 會根據系統自動計算每個整數所需的位元組數(通常是4個bytes)
// n*sizeof(int) 就是總共要分配的記憶體大小//回傳的記憶體位址是void*型別，因此需要轉型為int*
--------------------------------------------------------------
n = n * 2; //將原本的陣列大小n乘以2，準備擴充為2倍容量
array = (int *) realloc(array, n * sizeof(int));//使用realloc擴充原本的記憶體空間
//若原始空間不足，realloc會搬移到新位置並保留原資料
//回傳的新位址仍需轉型為 int*
for (int i = n / 2; i < n; i++) {
    array[i] = i + 1; // 初始化新元素
}
--------------------------------------------------------------
realloc 是否改變起始位址？
✅ 可能保持不變
- 如果原記憶體區塊後方有足夠空間可擴充，realloc 會直接延伸原區塊。
- 此時，起始位址不變，資料也不需搬移。
❌ 可能改變
- 如果原區塊後方空間不足，realloc 會：
- 在其他位置分配一塊更大的記憶體。
- 將原資料複製到新區塊。
- ⚠釋放原記憶體。
- 此時，起始位址會改變，你必須使用 realloc 的回傳值更新指標。
--------------------------------------------------------------
🧪begin()指向第一個元素
🧪rbegin()指向最後一個元素
🧪end()指向最後一個元素的下一個位置
🧪rend()指向第一個元素的前一個位置
🧪shrink_to_fit:reduce memory usage by freeing unused memory
🧪push_back():加入一個已建構好的物件
Person p1("Alice", 30); //加入現成物件  
people.push_back(p1);  // 複製 p1 進 vector
🧪emplace_back():直接在尾端建立物件 
people.emplace_back("Bob", 25);  // 直接建構，不需先建立物件
🧪emplace():在指定位置直接建構物件
people.emplace(people.begin(), "Charlie", 40);  // 插入到最前面
🧪append_range():一次加入多個元素
std::vector<Person> more = { {"David", 20}, {"Eva", 35} };
people.append_range(more);  // 將 more 的所有元素加入 people
🧪resize:調整容器大小
--------------------------------------------------------------
🔹 Insertion Sort 作法
🔧 步驟：
- 從第 1 個元素開始。
- 與前面元素比較，向左移動直到找到位置。
- 插入該元素。
- 重複直到結束。
🔹 Selection Sort 作法
🔧 步驟：
- 從第 0 個元素開始，找出剩下元素中的最小值。
- 與目前位置交換。
- 重複直到最後一個元素。
--------------------------------------------------------------
char string[6] = "Hello";     // string (with '\0' at the end)
char *words[3] = {"cat", "dog", "fish"};   // array of strings
--------------------------------------------------------------
 Unsorted array
 ◦ Linear search O(n)
 Sorted array
 ◦ Linear search O(n)
 ◦ Binary search O(logn)
--------------------------------------------------------------
Access、Update O(1)
Insert、Delete、Traverse O(n)
